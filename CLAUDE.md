# CLAUDE.md

Claude Code liest diese Datei automatisch, nicht `AGENTS.md`. Darum steht hier nur eines:

Lies zuerst `AGENTS.md` in diesem Ordner und halte dich daran. Es ist die einzige Regelquelle für KI-Werkzeuge in diesem Repository. Danach `agent/README.md` für die Leseordnung. Ohne eine Karte in `agent/use_cases/`, die READY ist, wird nichts gebaut.
